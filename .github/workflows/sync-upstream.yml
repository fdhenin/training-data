name: Sync from CrankAddict

on:
  schedule:
    - cron: '0 * * * *'  # toutes les heures
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch updated files from CrankAddict
        run: |
          BASE="https://raw.githubusercontent.com/CrankAddict/section-11/main"
          
          FILES=(
            "SECTION_11.md"
            "SKILL.md"
            "SETUP_ASSISTANT.md"
            "DOSSIER_TEMPLATE.md"
            "manifest.json"
            "changelog.json"
            "sync.py"
          )
          
          for FILE in "${FILES[@]}"; do
            curl -sf -o "$FILE" "$BASE/$FILE" && echo "✅ $FILE" || echo "⚠️ $FILE introuvable"
          done
          
          # Sync du dossier examples/ entier
          curl -sf "https://api.github.com/repos/CrankAddict/section-11/git/trees/main?recursive=1" | \
            python3 -c "
          import json,sys,os,urllib.request
          tree = json.load(sys.stdin)['tree']
          for f in tree:
              if f['path'].startswith('examples/') a
